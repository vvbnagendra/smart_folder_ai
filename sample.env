# --- Smart Folder Organizer Configuration ---

# Path(s) to scan, separated by a colon (e.g., /data/docs:/data/photos)
# In Docker, this should map to the volume mounted by the user.
SCAN_PATHS="C:/Users/LENOVO/Downloads/smart_folder/"

# Database URL for PostgreSQL metadata (optional, can use SQLite for minimal setup)
# DB_URL="postgresql://user:password@host:port/dbname"

# Vector database URL for Qdrant/Chroma
# QDRANT_URL="http://localhost:6333"

# OpenAI API Key for embeddings and optional LLM features
# OPENAI_API_KEY="YOUR_API_KEY"

# Model to use for embeddings (e.g., all-MiniLM-L6-v2) - will use a local one if not specified
EMBEDDING_MODEL="all-MiniLM-L6-v2"